{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://blemees/schemas/inbound/agent.interrupt.json",
  "title": "agent.interrupt",
  "description": "Cancel the in-flight turn on a session. SIGTERM → 500 ms → SIGKILL; the subprocess is then respawned with --resume. No-op if no turn is active (reply will carry was_idle:true).",
  "type": "object",
  "properties": {
    "type": { "const": "agent.interrupt" },
    "session_id": { "$ref": "https://blemees/schemas/_common.json#/$defs/SessionId" }
  },
  "required": ["type", "session_id"],
  "additionalProperties": false
}
