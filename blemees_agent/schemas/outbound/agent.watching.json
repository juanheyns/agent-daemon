{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://blemees/schemas/outbound/agent.watching.json",
  "title": "agent.watching",
  "description": "Ack for agent.watch. last_seq is the session's highest seq at subscribe time, matching agent.opened semantics.",
  "type": "object",
  "properties": {
    "type": { "const": "agent.watching" },
    "id": { "$ref": "https://blemees/schemas/_common.json#/$defs/RequestId" },
    "session_id": { "$ref": "https://blemees/schemas/_common.json#/$defs/SessionId" },
    "last_seq": { "type": "integer", "minimum": 0 }
  },
  "required": ["type", "session_id"],
  "additionalProperties": true
}
